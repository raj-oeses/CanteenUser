<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="college.canteen.collegecanteen">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".Canteen"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutUs"></activity>
        <activity android:name=".SignUpAdditional" >

        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".SignIn">


        </activity>
        <activity android:name=".FullKhanaFeed">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HotelProfile" />
        <activity android:name=".CanteenKhanaDetails" />
        <activity android:name=".UserProfile" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>